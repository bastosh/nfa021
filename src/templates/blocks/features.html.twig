<div class="grid-x grid-margin-x grid-margin-y medium-up-2 large-up-3 align-center">
    {% for feature in features %}
      <section class="cell card secondary padding-horizontal-2 padding-vertical-3 text-center text-uppercase font-bold">
        <h3 class="margin-bottom-0">{{ feature.title }}</h3>
      </section>
    {% endfor %}
</div>
