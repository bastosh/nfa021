<div class="grid-x grid-padding-x grid-margin-x grid-padding-y grid-margin-y medium-up-2 large-up-3 align-center">
    {% for feature in features %}
      <section class="cell card text-center bg-primary">
        <h3 class="card__title text-white margin-bottom-0">{{ feature.title }}</h3>
      </section>
    {% endfor %}
</div>
