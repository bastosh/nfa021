<div class="grid-x grid-padding-x grid-margin-x grid-padding-y grid-margin-y medium-up-2 large-up-3 align-center margin-top-2">
    {% for guide in guides %}
        <a class="cell card bg-primary" href="/fiches-conseils/{{ guide.id }}">
            <section class="text-center">
                <h2 class="card__title text-white margin-bottom-0">{{ guide.title }}</h2>
            </section>
        </a>
    {% endfor %}
</div>
