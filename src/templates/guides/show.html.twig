{% extends 'base.html.twig' %}

{% block title %}
    {{ page }}
{% endblock %}

{% block content %}

    <header class="header">
        {% include 'partials/nav.html.twig' %}
        <div class="content content-sm text-center">
            <h1>{{ guide.title }}</h1>
        </div>
    </header>

    <main class="text-container lead">
        {{ guide.description }}
    </main>

    {% include 'partials/footer.html.twig' %}
{% endblock %}


